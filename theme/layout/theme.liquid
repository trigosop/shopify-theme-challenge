<!doctype html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>
      {{ page_title }}
      {% if current_page != null %} &mdash; {{ shop.name }}{% endif %}
    </title>
    {{ content_for_header }}
    {{ 'main.css' | asset_url | stylesheet_tag }}
  </head>
  <body class="bg-slate-400">
    {{ content_for_layout }}

    <div id="container-app"></div>
    <script src="{{ 'tabs.js' | asset_url }}" defer></script>
  </body>
</html>
